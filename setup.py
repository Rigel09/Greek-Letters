from setuptools import setup, find_packages

setup(
    name="GreekLetters",
    version="0.1.0",
    description="Contains unicode for upper and lower case greek letters",
    packages=["GreekLetters"],
)
